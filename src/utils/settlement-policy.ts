/**
 * settlementPolicy — Re-exports from canonical service layer.
 *
 * @deprecated Import directly from '@/services/settlementService' instead.
 * This file exists only for backward compatibility.
 */

export { isReconciliationOnly, getPushBlockReason } from '@/services/settlementService';
